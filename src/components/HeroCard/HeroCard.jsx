import { Col } from "react-bootstrap";
import "./HeroCard.css"
const HeroCard = ({data}) => {
    console.log(data.img)
    return (
        <Col className="hero-card">
            
        </Col>
    );
};

export default HeroCard;